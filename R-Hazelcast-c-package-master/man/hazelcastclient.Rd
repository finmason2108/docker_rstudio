% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazelcastclient-exports.R
\name{HazelcastClient}
\alias{HazelcastClient}
\title{Hazelcast client backed by the Hazelcast C++ client}
\arguments{
\item{ip}{character Address of a cluster member.}

\item{clusterName}{character Name of the Hazelcast cluster.}

\item{port}{integer Port of the cluster member. Defaults to 5701.}
}
\value{
A \code{HazelcastClient} object from the \emph{C++} HazelcastClient class.
}
\description{
An Rcpp module class wrapping the Hazelcast C++ client. Values are stored and
retrieved as raw vectors, so callers serialise and unserialise R objects
themselves.
}
\section{Methods}{

\describe{
\item{\code{GetUrl()}}{Address, port and current map name, as used in error messages.}
\item{\code{SetMap(mapName)}}{Select the distributed map used by \code{Get}, \code{TryGet} and \code{Put}.}
\item{\code{SetVerboseMode(verboseMode)}}{Print a line on each successful fetch.}
\item{\code{Get(key)}}{Return the stored raw vector. Throws if the key is absent.}
\item{\code{TryGet(key)}}{Return the stored raw vector, or an empty vector if the key is absent.}
\item{\code{Put(key, value)}}{Store a raw vector under \code{key}.}
}
}

\examples{
\dontrun{
hz <- new(HazelcastClient, ip = "10.30.100.180", clusterName = "DEV", port = 5701)
hz$SetMap("binarymap")
hz$Put("42:asset_info", serialize(data.frame(a = 1), NULL))
unserialize(hz$Get("42:asset_info"))
}
}
